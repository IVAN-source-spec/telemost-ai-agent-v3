[project]
name = "telemost-ai-agent-v3"
version = "0.1.0"
description = ""
authors = [
    {name = "IVAN-source-spec",email = "iv.slavinsky@razum.life"}
]
requires-python = ">=3.12, <4.0"
dependencies = [
    "fastapi (>=0.137.1,<0.138.0)",
    "uvicorn[standard] (>=0.49.0,<0.50.0)",
    "qrcode[pil] (>=8.2,<9.0)",
    "playwright (>=1.60.0,<2.0.0)",
    "sounddevice (>=0.5.5,<0.6.0)",
    "scipy (>=1.18.0,<2.0.0)",
    "numpy (>=2.5.0,<3.0.0)",
    "pyaudio (>=0.2.14,<0.3.0)",
    "imageio-ffmpeg (>=0.6.0,<0.7.0)",
    "huggingface-hub (>=1.21.0,<2.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
